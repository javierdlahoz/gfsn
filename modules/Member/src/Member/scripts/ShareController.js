angular.module('gfsn').controller('ShareController', ShareController);

function ShareController($scope) {
	console.log('share controller initialized');
	var self = this;
	self.emails = 'javier; kurt';
		
}